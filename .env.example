# Copy this file to .env, or edit the .env in VoiceCodex's Application Support folder.
# VoiceCodex reads these values directly; this file is never sourced as a shell script.
# Keep your real .env local. It is ignored by Git.
TYPESAFE_API_KEY=
TYPESAFE_DEFAULT_MODEL=jev-1.13.0
SONIOX_API_KEY=
